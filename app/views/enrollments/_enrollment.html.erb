

<% if current_user.role == "student" %>
<p><%= "Currently" %>&emsp;<%= link_to "Drop?", [@course, enrollment]%>
</p>
<% else %>
  <% user = User.where(user_id: enrollment.student_id)[0].name %>
  <p><%= link_to user, [@course, enrollment]%> &emsp;
  </p>
<% end %>